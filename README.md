# qualified-blog-scraper

Utility to scrape the Qualified blog to facilitate accurate importing to webflow.

Requires Node. Usage:

```
npm i
npm start
```

Give it some time to run. Outputs data in JSON and CSV.
